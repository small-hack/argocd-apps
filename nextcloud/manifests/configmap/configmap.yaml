apiVersion: v1
kind: ConfigMap
metadata:
  name: nextcloud-hostname
  namespace: nextcloud
spec: {}
status:
  # Duck-typing ignores all other aspects of the resource except 
  # the "decisions" list
  decisions:
  - clusterName: in-cluster
    nextcloudHostname: cloud.social-media-for-dogs.com
