---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nextcloud-config
spec:
  storageClass: seaweedfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
