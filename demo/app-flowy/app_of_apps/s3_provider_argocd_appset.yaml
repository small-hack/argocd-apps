---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: appflowy-s3-provider-app-set
  namespace: argocd
spec:
  goTemplate: true
  # generator allows us to source specific values from an external k8s secret
  generators:
    - plugin:
        configMapRef:
          name: secret-var-plugin-generator
        input:
          parameters:
            secret_vars:
              - appflowy_s3_provider
  template:
    metadata:
      name: appflowy-{{ .appflowy_s3_provider }}
      annotations:
        argocd.argoproj.io/sync-wave: "2"
    spec:
      project: appflowy
      destination:
        server: "https://kubernetes.default.svc"
        namespace: appflowy
      syncPolicy:
        syncOptions:
          - ApplyOutOfSyncOnly=true
        automated:
          prune: true
          selfHeal: true
      source:
        repoURL: https://github.com/small-hack/argocd-apps.git
        path: demo/appflowy/storage/{{ .appflowy_s3_provider }}/
        targetRevision: main
