---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: juicefs-storage-class-credentials
  namespace: juicefs
  labels:
    juicefs.com/validate-secret: "true"
spec:
  target:
    name: juicefs-storage-class-credentials
    deletionPolicy: Delete
    template:
      type: Opaque
      data:
        name: juicefs
        metaurl: |-
          {{ `{{ .valkeyUrl }}`}}
        access-key: |-
          {{ `{{ .accessKeyId }}` }}
        secret-key: |-
          {{ `{{ .secretAccessKey }}` }}
        bucket:: |-
          {{ `{{ .bucket }}` }}
  data:
    - secretKey: accessKeyId
      sourceRef:
        storeRef:
          name: bitwarden-login
          kind: ClusterSecretStore
      remoteRef:
        key: {{ .Values.juicefs_s3_admin_credentials_bitwarden_id }}
        property: username

    - secretKey: secretAccessKey
      sourceRef:
        storeRef:
          name: bitwarden-login
          kind: ClusterSecretStore
      remoteRef:
        key: {{ .Values.juicefs_s3_admin_credentials_bitwarden_id }}
        property: password

    - secretKey: bucket
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: {{ .Values.juicefs_s3_admin_credentials_bitwarden_id }}
        property: bucket

    - secretKey: metaurl
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: {{ .Values.juicefs_valkey_bitwarden_id }}
        property: metaurl
