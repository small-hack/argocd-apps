---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: workflows-s3-pvc-app-set
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  goTemplate: true
  # generator allows us to source specific values from an external k8s secret
  generators:
    - plugin:
        configMapRef:
          name: secret-var-plugin-generator
        input:
          parameters:
            secret_vars:
              - workflows_s3_provider
              - workflows_s3_backup_endpoint
              - workflows_s3_backup_bucket
              - workflows_s3_backup_region
              - workflows_s3_pvc_capacity
              - workflows_pvc_backup_schedule
  template:
    metadata:
      name: workflows-s3-pvc
      annotations:
        argocd.argoproj.io/sync-wave: "1"
    spec:
      project: argo-cd
      destination:
        server: "https://kubernetes.default.svc"
        namespace: argocd
      syncPolicy:
        syncOptions:
          - ApplyOutOfSyncOnly=true
        automated:
          prune: true
          selfHeal: true
      source:
        repoURL: https://github.com/small-hack/argocd-apps.git
        path: s3_persistence_and_backups/
        targetRevision: main
        helm:
          valuesObject:
            provider: '{{ .workflows_s3_provider }}'
            pvc_capacity: '{{ .workflows_s3_pvc_capacity }}'

            seaweedfs:
              master_pvc: false

            # not in use yet
            k8up:
              backup_name: "workflows-nightly-backup"

              securityContext:
                runAsUser: 0

              # -- can be set to 's3' or 'local'
              backup_type: "s3"

              s3:
                bucket: '{{ .workflows_s3_backup_bucket }}'
                endpoint: '{{ .workflows_s3_backup_endpoint }}'
                accessKeyIDSecretRef:
                  name: 's3-backups-credentials'
                  key: 'accessKeyId'
                  optional: false
                secretAccessKeySecretRef:
                  name: 's3-backups-credentials'
                  key: 'secretAccessKey'
                  optional: false

              repoPasswordSecretRef:
                name: 's3-backups-credentials'
                key: 'resticRepoPassword'

              prometheus_url: 'push-gateway.prometheus.svc:9091'

              schedules:
                backup: '{{ .workflows_pvc_backup_schedule }}'
                check: "0 2 * * *"
                prune: "0 2 * * *"
