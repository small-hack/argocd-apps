---
apiVersion: v1
kind: ConfigMap
metadata:
  name: alloy-metrics-cfg
data:
  config.alloy: |-
    {{ range $path, $_ :=  .Files.Glob  "alloy_files/metrics-config.alloy" }}
    {{- $.Files.Get $path | nindent 4 }}
    {{ end }}
