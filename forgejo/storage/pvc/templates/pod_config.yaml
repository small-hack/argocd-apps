apiVersion: k8up.io/v1
kind: PodConfig
metadata:
  name: backups-podconfig
spec:
  template:
    spec:
      {{- with .Values.k8up.securityContext }}
      securityContext:
        runAsUser: {{ .runAsUser }}
      {{- end }}
      {{- range .Values.k8up.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- range .Values.k8up.tolerations }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        fsGroup: {{ .Values.k8up.podSecurityContext.runAsUser }}
        runAsUser: {{ .Values.k8up.podSecurityContext.runAsUser }}
      containers:
        - name: test
          env:
          - name: FORGEJO
            value: 'true'
