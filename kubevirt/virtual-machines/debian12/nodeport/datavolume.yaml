---
# The datavolume is the VM's harddrive
apiVersion: cdi.kubevirt.io/v1
kind: DataVolume
metadata:
  name: vm1-dv
  namespace: vm1
spec:
  source:
    pvc:
      namespace: default
      name: debian12-pvc
  pvc:
    accessModes:
      - ReadWriteMany
    resources:
      requests:
        storage: 120Gi
