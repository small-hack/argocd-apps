---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: jellyfin-bitwarden-eso
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  goTemplate: true
  # generator allows us to source specific values from an external k8s secret
  generators:
    - plugin:
        configMapRef:
          name: secret-var-plugin-generator
        input:
          parameters:
            secret_vars:
              - global_external_secrets
              - jellyfin_s3_backups_credentials_bitwarden_id
  template:
    metadata:
      name: jellyfin-bitwarden-eso
      annotations:
        argocd.argoproj.io/sync-wave: "1"
    spec:
      project: jellyfin
      destination:
        server: https://kubernetes.default.svc
        namespace: jellyfin
      syncPolicy:
        syncOptions:
          - ApplyOutOfSyncOnly=true
          - Retry=true
        automated:
          prune: true
          selfHeal: true
      source:
        repoURL: 'https://github.com/small-hack/argocd-apps.git'
        path: jellyfin/external_secrets/
        targetRevision: main
        helm:
          releaseName: jellyfin-bitwarden-eso
          valuesObject:
            provider: '{{ .global_external_secrets }}'
            s3BackupCredentialsBitwardenID: "{{ .home_assistant_s3_backups_credentials_bitwarden_id }}"
