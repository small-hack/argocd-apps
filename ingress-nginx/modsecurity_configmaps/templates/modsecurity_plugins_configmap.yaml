apiVersion: v1
kind: ConfigMap
metadata:
  name: modsecurity-plugins
data:
  empty-after.conf: |
    # no data
  empty-before.conf: |
    # no data
  empty-config.conf: |
    # no data

{{ range $path, $_ :=  .Files.Glob  "modsecurity_exception_files/**.conf" }}
  {{ $.Files.Get $path }}: |
    beepboop
{{ end }}
