postgres_pvc:
  # -- default storage class name to use for config pvc
  storageClassName: "local-path"
  # -- default access mode for the config pvc
  accessMode: "ReadWriteOnce"
  # -- storage capacity for the harbor config pvc
  storage: "5Gi"

rgistry_pvc:
  # -- default storage class name to use for config pvc
  storageClassName: "local-path"
  # -- default access mode for the config pvc
  accessMode: "ReadWriteOnce"
  # -- storage capacity for the harbor config pvc
  storage: "64Gi"

jobs_pvc:
  # -- default storage class name to use for config pvc
  storageClassName: "local-path"
  # -- default access mode for the config pvc
  accessMode: "ReadWriteOnce"
  # -- storage capacity for the harbor config pvc
  storage: "2Gi"

trivy_pvc:
  # -- default storage class name to use for config pvc
  storageClassName: "local-path"
  # -- default access mode for the config pvc
  accessMode: "ReadWriteOnce"
  # -- storage capacity for the harbor config pvc
  storage: "5Gi"

valkey_pvc:
  # -- default storage class name to use for config pvc
  storageClassName: "local-path"
  # -- default access mode for the config pvc
  accessMode: "ReadWriteOnce"
  # -- storage capacity for the harbor config pvc
  storage: "1Gi"

# -- tolerate affinity
affinity: {}

# -- tolerate taints
tolerations: []

# -- for enabling backups to a remote s3 provider or local disk backup
k8up:
  # -- user to run the backups as
  securityContext:
    runAsUser: 0
