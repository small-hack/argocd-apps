---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: registry
  annotations:
    k8up.io/backup: 'true'
spec:
  storageClassName: {{ .Values.harbor_pvc.storageClassName }}
  accessModes:
    - {{ .Values.harbor_pvc.accessMode }}
  resources:
    requests:
      storage: {{ .Values.harbor_pvc.storage }}
