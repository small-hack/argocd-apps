---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jobs
  annotations:
    k8up.io/backup: 'true'
spec:
  storageClassName: {{ .Values.jobs_pvc.storageClassName }}
  accessModes:
    - {{ .Values.jobs_pvc.accessMode }}
  resources:
    requests:
      storage: {{ .Values.jobs_pvc.storage }}
