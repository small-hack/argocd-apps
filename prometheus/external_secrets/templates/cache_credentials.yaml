---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: valkey-chunk-credentials
spec:
  target:
    # Name for the secret to be created on the cluster
    name: valkey-chunk-credentials
    deletionPolicy: Delete
    template:
      type: Opaque
      data:
        valkey_password: |-
          {{ `{{ .password }}` }}
  data:
    # Key given to the secret to be created on the cluster
    - secretKey: password
      sourceRef:
        storeRef:
          name: bitwarden-login
          kind: ClusterSecretStore
      remoteRef:
        key: {{ .Values.lokiValkeyCredentialsBitwardenID }}
        property: password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: valkey-index-credentials
spec:
  target:
    # Name for the secret to be created on the cluster
    name: valkey-index-credentials
    deletionPolicy: Delete
    template:
      type: Opaque
      data:
        valkey_password: |-
          {{ `{{ .password }}` }}
  data:
    # Key given to the secret to be created on the cluster
    - secretKey: password
      sourceRef:
        storeRef:
          name: bitwarden-login
          kind: ClusterSecretStore
      remoteRef:
        key: {{ .Values.lokiValkeyCredentialsBitwardenID }}
        property: password
