apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mastodon-postgres
  namespace: mastodon
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 25Gi
