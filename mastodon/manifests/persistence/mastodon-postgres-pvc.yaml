apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mastodon-postgres
  namespace: mastodon
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
