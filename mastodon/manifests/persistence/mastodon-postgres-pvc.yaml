apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mastodon-postgres
  namespace: mastodon
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
