apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mastodon-assets
  namespace: mastodon
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 64Gi
