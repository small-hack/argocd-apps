apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mastodon-assets
  namespace: mastodon
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 64Gi
