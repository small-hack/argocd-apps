apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mastodon-system
  namespace: mastodon
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
