apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mastodon-system
  namespace: mastodon
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 64Gi
