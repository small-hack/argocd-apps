apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mastodon-system
  namespace: mastodon
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Gi
