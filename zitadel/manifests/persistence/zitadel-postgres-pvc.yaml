apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zitadel-database
  namespace: zitadel
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
