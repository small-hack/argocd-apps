apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zitadel-postgres
  namespace: zitadel
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
